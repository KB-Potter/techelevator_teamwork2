package com.techelevator;

import java.math.BigDecimal;

public class CheckingAccount extends BankAccount {
	
	@Override
	  public BigDecimal withdraw(BigDecimal amountToWithdraw) {
		if(getBalance().compareTo(new BigDecimal(-90 <= 0))) {
			return getBalance();
		} else {
			if(getBalance().compareTo(new BigDecimal( 100 ) < 0 )) {
			return getBalance();
		} else { 
			if (getBalance().compareTo(new BigDecimal()))
		} else if (getBalance().compareTo(new BigDecimal("0.00"))) == 0 { 
	 } else if { (getBalance().compareTo(new BigDecimal("0.00")))
	 }
		 	
	 }
	 }
	 
	 
	 }
